# BNSF 现在打的是免登录移动端接口，不需要账号密码了。
# 可选的超时/批量配置（不填有默认值）：
# BNSF_DEFAULT_TIMEOUT=30000
# BNSF_NAVIGATION_TIMEOUT=30000
# BNSF_SELECTOR_TIMEOUT=10000
# BNSF_BATCH_SIZE=50

# 存储用的 Google Sheet ID（表格 URL 里 /d/ 和 /edit 之间那一段）——
# 用 `npx tsx src/dev/setup-sheet.ts` 建表，把打印出来的 ID 填在这里。
# 本地开发认证：gcloud auth application-default login（要带 spreadsheets/drive scope，见 CLAUDE.md）。
SHEET_ID=

# 数据备份用的 Cloud Storage bucket——不填就用默认值 draye-mvp-backups，所有客户部署
# 共用一个 bucket（对象路径按 SHEET_ID 分区，不会互相覆盖），一般不需要改这个。
# BACKUP_BUCKET=draye-mvp-backups
